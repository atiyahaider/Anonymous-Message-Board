* {
  margin: 0;
  padding: 0;
  border: box-border;
}

#container{
  background-color: black;
  background-image: linear-gradient(#585858, black);
  background-size: cover;
  height: 100vh;
}

#header {
  width: 100%;
  height: 140px;
  background-image: linear-gradient(#990000 10%, #190000);
  transform: skewY(-2.75deg);
  transform-origin: top left;
  overflow: hidden;  
  padding-top: 65px;
  font: 40px "Arial";
  text-align: center;
  /*transform: skewY(2.75deg);*/
  color: white;
}

/* side line 1*/
#container:after {
  content: '';
  position: absolute;
  top: 0;
  left: 10px;
  height: 85%;
  width: 0.3em;
  background-image: linear-gradient(black 40%, #990000);
  transform-origin: top;
  transform: perspective(8px) rotateY(2deg);
}

/* side line 2 */
#content:after {
  content: '';
  position: absolute;
  top: 0px;
  left: 25px;
  height: 80%;
  width: 0.3em;
  background-image: linear-gradient(black 30%, #990000);
  transform-origin: top;
  transform: perspective(8px) rotateY(2deg);
}

#content {
  text-align: center;
}

table {
  border: 1px solid black; 
}

#footer {
  position: fixed;
  left: 0;
  bottom: 0;
  width: 100%;
  height: 60px;
  text-align: center;
  background-image: linear-gradient(#484848, #190000);
  transform: skewY(1.5deg);
  transform-origin: bottom left;
  padding-top: 5px;
  padding-left: 10px;
  font: 0.95em "Arial";
  text-align: left;
  transform: skewY(-1.5deg);
  color: white;
}

.replies {
  margin-left: 50px;
}

.reply {
  border: 1px dotted black;
  margin: 5px;
  padding: 5px;
}

.newReply {
  margin: 10px 5px 10px 5px;
}

.thread {
  border: 2px solid black;
  padding: 5px;
  margin-bottom: 5px;
}

.id {
  font-size: 10.5px;
}

#submitNewThread {
  margin: 50px;
}